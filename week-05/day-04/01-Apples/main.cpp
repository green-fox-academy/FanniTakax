#include <iostream>

int main() {


    //Create a single function (eg. getApple()) that returns a string (eg. "apple")
    //Create a test for that.
    //Create a test method
    //Use the ASSERT_EQ
    //The expected parameter should be the same string (eg. "apple")
    //The actual parameter should be the return value of the method (eg. getApple())
    //Run the test
    //Change the expected value to make the test failing
    //Run the test
    //Fix the returned value to make the test succeeding again
    //______________________________________________________________________________
    //    //Create a sum method in the same project next to the getApple() which has a vector of integers as parameter
    //    //It should return the sum of the elements in the list
    //    //Follow these steps:
    //    //Add a new test case
    //    //create a list of integers
    //    //use the ASSERT_EQ to test the result of the created sum method
    //    //Run them
    //    //Create different tests where you
    //    //test your method with an empty list
    //    //with a list with one element in it
    //    //with multiple elements in it
    //    //with negative sum
    //    //Run them
    //    //Fix your code if needed


    return 0;
}