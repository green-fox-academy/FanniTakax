#include <iostream>

int main() {

    //Create a sum method in the same project next to the getApple() which has a vector of integers as parameter
    //It should return the sum of the elements in the list
    //Follow these steps:
    //Add a new test case
    //create a list of integers
    //use the ASSER_EQ to test the result of the created sum method
    //Run them
    //Create different tests where you
    //test your method with an empyt list
    //with a list with one element in it
    //with multiple elements in it
    //with negative sum
    //Run them
    //Fix your code if needed


    return 0;
}