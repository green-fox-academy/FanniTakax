#include "plant.h"

Plant::Plant(int currentWaterAmount, const std::string &color) : _currentWaterAmount(currentWaterAmount),
                                                                 _color(color) {}
