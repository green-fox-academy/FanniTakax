#include <iostream>
#include <string>

// Given a string, compute recursively a new string where all the
// adjacent chars are now separated by a '*'.
std::string letterChange(std::string text);

int main() {

    std::cout << letterChange("valamit ide is kell irni") << std::endl;

    return 0;
}

std::string letterChange(std::string text, int i){


     //insert, es hogy hova. a hova-t valtoztatom. majd.
}


